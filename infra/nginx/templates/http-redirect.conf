# 证书已就绪：80 仅 ACME + 其余跳转到 HTTPS
server {
    listen 80;
    server_name somna-ai.com www.somna-ai.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
